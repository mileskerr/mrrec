dependencies: SDL2, SDL2_ttf
build instructions: run `make` in the repo directory

right mouse or arrow keys to rotate camera, WASD + lctrl/lshift to move camera.

Usage:
    mrrec [-m <path/to/model.obj>] [-w <width>] [-h <height>] [-F <max fps>]
